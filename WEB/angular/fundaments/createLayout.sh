# Layout is a component, so the method is almost the same

ng g c layoutsDirectory/layout-name

# where app folder is the root folder so the directory needs to be inside the app folder.

# needs to add routes
