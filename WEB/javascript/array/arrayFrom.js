/* Cria uma nova instância de um array 
a partir de um parâmetro array-like
ou iterable project */

// pode ser um node.list (querySelector)

const divs = document.querySelectorAll('div')
const divArray = Array.from(divs)


